---
description: IndustrialEmbedded 完整项目阅读、状态判断与架构说明约定
alwaysApply: true
---

# IndustrialEmbedded 项目阅读约定

- 阅读项目时必须按完整 M0～M7 总体架构理解，不只根据当前已实现文件或当前里程碑回答。优先读取 `Docs/03_DEV_PROCESS.md`、`Docs/01_PROJECT_OVERVIEW.md`，再检查相关源码、工程配置和测试记录。
- 每次说明进度时都要明确：当前总体目标、已实现模块、源码/构建/板级证据、未实现或未验证内容，以及它们在完整项目中的上下游关系。
- 对尚未实现的模块，必须说明其计划中的接口、数据流、任务所有权、硬件/协议连接和验收方式；“未实现”不等于“与项目无关”，也不能把设计说明写成已完成。
- 例如 M7 低功耗应解释为：`0x03AA` 由 ProtocolTask/ControlTask 发起睡眠请求，PowerManager 协调 StorageTask flush/关文件、停止 ADC/DMA、等待 RS485 空闲、保存使能状态，再配置 RTC/EXTI、调整看门狗并进入 WFI；唤醒后恢复时钟/外设/任务与定时器，重置周期基准并发送 `0x0381`。当前未实现时必须同时指出缺失的代码入口和验证缺口。
- 严格区分“已实现”“已构建”“已板测”“仅有设计/源码证据”“未验证”“项目决定暂缓/跳过”；不得用成功构建替代运行或板级证据。
- 当前项目结项范围是 M0～M6 的选定范围；M7 低功耗和总验收因实习求职安排暂不继续开发或测试。M6 的 N-05、N-07、N-08 随机掉电项保持未执行，不能写成全部 N 项通过。
- 回答后续实现建议时，先指出该模块在完整架构中的位置和依赖，再给最小下一步；不要脱离项目总目标只给孤立代码或局部结论。
